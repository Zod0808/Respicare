# RespiCare Mobile PWA - Demo local en navegador
# Uso: `npm run demo` (equivale a `next dev -p 8083` con este env)
# El backend y AI corren en localhost via docker-compose.dev.yml (setup-demo)

NEXT_PUBLIC_API_URL=http://localhost:3001/api/v1
NEXT_PUBLIC_AI_SERVICE_URL=http://localhost:8000
